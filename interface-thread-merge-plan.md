# Eliminate the ahk-h interfaceThread (module-interface.ahk)

## Context

QPV depends on AutoHotkey_H v1.1 solely to host `lib/module-interface.ahk` as a second in-process interpreter (`interfaceThread := ahkthread(...)`, quick-picto-viewer.ahk:456/458 — the comment at :460 states the intent: a responsive UI while main is busy). That thread owns the main window (PVhwnd), the four layered GDI view windows, the menu bar, tooltip/flyout/halo windows, live input capture (mouse, pen pressure, gestures, keyboard forwarding), the slideshow clock, the taskbar/DWM integration, and the app's exit state machine — while the main thread does all logic and all painting into those windows. Goal (user-confirmed): eliminate ahk-h entirely — merge the module into the main thread, switch to stock AutoHotkey v1.1 + official Ahk2Exe, and keep cancellation/abort working via pump-shims (no async-decode work in this plan; the dissociated-UI wish is consciously traded away).

**QPV = quick-picto-viewer.ahk (106,555 ln); MI = lib/module-interface.ahk (4,823 ln, ~1,350 of them `class taskbarInterface`).**

## Why merge — the answer to "are there other possible solutions?"

Plain AHK v1.1 has exactly one script thread; in-process GUI-on-a-second-thread is precisely the feature ahk-h added. Without ahk-h, "UI dissociated from the main thread" has only two shapes, and no third simpler trick exists:

1. **A second process** (plain-AHK interface exe; RPC over WM_COPYDATA/SendMessage or COM). Rejected: it preserves the polling architecture and requires a ~315-site transport rewrite, plus cross-process painting (main's `UpdateLayeredWindow` onto foreign-process hwnds is fragile; the clean fix is a shared-DIB present redesign), cross-process ownership/z-order/focus coordination, and crash/lifecycle management. The merge, by contrast, *deletes* 233 of those 315 sites outright (167 assigns + 66 reverse calls become plain globals/direct calls). The facade layer (Phase B) keeps this option swappable later at low cost if ever wanted.
2. **A C++ UI thread in qpvmain.dll.** Rebuilding chrome in Win32 is out of proportion; a narrow "input-mirror + present" variant dissolves once you notice async input reads + selective message pumping cover the paint-loop needs in-thread.

Everything else (ObjRegisterActive in-process, hidden-window RPC between pseudo-threads, v2 rewrite) is not parallelism or out of scope. **Decision: full merge.** Bonus: the merge removes 100% of the AHK_H dependency (verified — the 315 bridge sites + `ahkthread()` are the only H APIs in the repo) and structurally fixes existing bugs: the exit path's `Process, Close` fired ~10ms after `TrueCleanup` starts (racing the seen-images DB COMMIT, QPV:4457), the GUI-restart pref-blanking (restartEntireGui QPV:73224 passes 9 of 16 externObj fields), and the unwatched-thread-death failure mode.

## Verified architecture facts that shape the work

- **Painting is a non-issue**: one funnel, `doLayeredWinUpdate()` QPV:5826 (sole `UpdateLayeredWindow` at :5866), already main-thread. MI never paints.
- **Boundary is scalar-only**: main→iface 16 `ahkFunction` (only `BuildGUI`/`msgBoxWrapper` use returns), 45 `ahkPostFunction` (25 names), 167 `ahkassign` (41 mirrored globals), 14 `ahkgetvar` (9 vars); iface→main (`MainExe := AhkExported()`, MI:20) 40 posts + 15 gets + 11 assigns, all async. Sole pointer (`UserMemBMP` QPV:77325) is truthiness-tested only (MI:1889). Largest post = 4 args (H API caps at 10 string args — verified from the repo's ahk_h zip).
- **Liveness polls exist because paint loops hold `Critical, on` and never pump** (documented MI:69-73): `LbtnDwn` via `determineLClickState()` QPV:4298 (54 sites; already ORs `GetKeyState("LButton")` — cross-thread read is droppable); `penPressureRaw` via `getBrushPenPressure()` QPV:4306 (2 sites, inside the brush loops QPV:78621/79236; producer `WM_PENpressure` MI:1215 on WM_POINTER 0x0245-0x024A); abort flags set by iface input handlers and polled between work stages: `canCancelImageLoad` (QPV:74273/75731/75803/75828/75885), `alterFilesIndex` (QPV:85687), `colorPickerMustEnd` (QPV:48591), `animGIFplaying` (QPV:12370), and `determineTerminateOperation()` QPV:35425 which spin-waits while the iface thread shows the abort dialog (`askAboutStoppingOperations` MI:1034). One qpvmain.dll call in MI: `dupesEngineCancel` (MI:1030) = sqlite3_interrupt while main is inside `sqlite3_step`.
- **OnMessage string-mode REPLACES, never chains** (v1 legacy mode). 8 message numbers are registered on both sides (0x100/0x104/0x200/0x201/0x202 name/case-clashed diverged bodies; 0x203/0x20A/0x20E entirely different handlers — QPV:443-452 vs MI:57-98). Today's keyboard path itself relies on replacement semantics (MI:83-84 beep handlers on 0x100-0x108, then MI:86-87 re-registers 0x100/0x104). Unification is a hard prerequisite of the flip.
- **Collisions**: 37 function names in QPV∩MI (8 identical, 5 near-identical, 6 parallel-evolution, 18 different-job — e.g. `WinClickAction` 15-line iface dispatcher vs 645-line main body), +16 vs shell-stuff.ahk, +3 MDMF_* vs Gdip_All.ahk, +1 vs msgbox2.ahk (~57 total); 3 real SetTimer-name collisions (`mouseTurnOFFtooltip`, `updateUIctrl`, `PreProcessKbdKey`; `theSlideShowCore` main-side refs are commented); labels `GuiClose:/doCleanup:/mouseToolTipGuiaGuiClose/Escape`; GUI name `mouseToolTipGuia` used independently by both; **`PVmenu` is two different menus** (iface = menu bar MI:2240; main = context menu QPV:69085+); iface Gui numbers 1-5 vs main's 1,059 bare `Gui, Add` + ~22 bare `Gui, +Option` lines relying on named defaults.
- **Directive divergence is load-bearing**: main `DetectHiddenWindows On` (QPV:65) vs MI default Off — `identifyMenus()` (MI:2733) matches the *hidden* `#32768` window under DHW-On, which would turn the reader `#If` block's bare hotkeys (`RButton::`, `Left::`, `Space::`… MI:2740-2796) into system-wide key theft. **P0 fix.** Also `#IfTimeout` 35 vs 2000; `#Persistent` (MI only); the `detectToolbar` (MI:333) / `adjustCanvas2Toolbar` (QPV:97631) deliberate twins exist because of DHW.
- **Menus**: bar clicks post 23 dynamic `InvokeMenuBar*` names to main; main's builders (e.g. `InvokeMenuBarFile` QPV:66258) already JIT-rebuild dropdowns from current state, then block in `Menu, Show` (showThisMenu manubarMode=1, QPV:69939). The MSAA hover-switch/reader machinery (60ms timer MI:2415, F10 SendInput, 9-deep probe MI:2064, `constantMenuReader` MI:2860) exists because same-thread timers/hotkey subroutines don't run during the modal menu loop. MI:90-94 shows prior (commented) experiments with WM_MENUSELECT-style monitoring — the intended direction.
- **Exit path runs through MI**: `GuiClose` → `byeByeRoutine` MI:1808 (escalating veto machine reading 16 of the 41 mirrored globals) → posts `TrueCleanup` to main and hard-kills the PID via `TimerExit` MI:1910.
- **Slideshow**: one-shot iface timer `theSlideShowCore` MI:1195, re-armed only by main's post after each completed load (`dummySlideshow` MI:1186); GIF frame timer is already main-side (`autoChangeDesiredFrame` QPV:12332).
- **Post-merge freeze inventory** (accepted per user decision; pump-shims keep cancel working): #1 per-navigation sync decode (`LoadBitmapForScreen` QPV:75159; seconds for RAW/PDF/huge), #2 `GetFilesList` QPV:88151, #3 thumbs harvest spin (QPV:85750, `Critical` whole function), fades, filter applies, saves, dupes/batch stepping loops (cooperative — pumpable), `VACUUM` QPV:73626 + sqlite sorts (cancel restored in D3).
- Dead/cleanup: `lib/module-fim-thumbs.ahk` fully dead (delete); `lib/shell-stuff.ahk:1699` posts `setMenusTheme` cross-thread from inside the same-named function → self-recursion post-merge (delete the forward line); `;@Ahk2Exe-AddResource` QPV:40 + `GetRes` (shell-stuff:1316) exist only to feed `ahkThread` source text when compiled.

## Phases (each ends at a hand-testable checkpoint; dev is on Linux, manual testing on Windows)

### Phase P — Windows probe scripts (run any time before C; they size D and C's keep-list)
Round 1 (p1-p6) **RUN 2026-08-31** on stock v1.1 — verdicts: P1 FAIL (WM_MENUSELECT monitor never fires during same-thread `Menu, Show`); P2 FAIL (WM_INITMENUPOPUP monitor never fires — and this was a NATIVE Gui menu bar, so the blocking covers the exact loop D1 targets); P3 confirmed timers don't tick; P4 ambiguous log but no visible SendInput effect (auto-classifying version now in the repo for re-runs); P5 **reception PASS** — the WH_MSGFILTER raw callback runs during the loop and logged MSGF_MENU traffic, but SendInput from inside it moved nothing; P6 FAIL (consistent with P1). **Empirical rule adopted by this plan: in one stock-v1 interpreter, no AHK pseudo-thread launches happen while any menu of that interpreter is being tracked — OnMessage monitors, timers, and hotkey subroutines are all blocked, for `Menu, Show` popups and native bar tracking alike; raw Win32 callbacks (hooks) DO run.** This is precisely why the app has needed the second ahk-h interpreter for its menu machinery, and it does not reshake the merge decision — GIF frames and slideshow advances already stall during menus today (main-side timers, main shows the dropdowns), so nothing outside the menu subsystem changes character.
Round 2 **RUN 2026-08-31 (same day)** — verdicts: p4 re-run FAIL (no hotkey subroutine response at all — the reader `#If` block is definitively non-viable post-merge); p5 re-run census: **wheel 0, keydown 8, rbutton 2** — wheel does NOT traverse MSGF_MENU, which fully explains p9/p10 failing; **p7 FULL PASS** (CALLWNDPROC sees WM_MENUSELECT and a tooltip GUI created/updated from the raw callback follows both mouse-hover and arrow-key highlight changes); **p8 FULL PASS** (AHK Menu rebuild from the callback at WM_INITMENUPOPUP time works — JIT dropdowns confirmed); p9/p10 FAIL (no wheel input to act on); **p11 FULL PASS** (SetWinEventHook alternative also works). **D1 and D2 mechanisms are settled**: WH_CALLWNDPROC for tracking/announcements + JIT rebuild; MSGFILTER provably sees RButton and keydowns for in-menu input shaping; the only open detail is wheel → round 3 probe **p12** (WH_GETMESSAGE — sees every message the modal loop retrieves, PM_REMOVE'd messages legally modifiable in place; rewrite wheel→WM_KEYDOWN; if even it sees no wheel, the in-menu wheel feature is dropped as an accepted degradation since native Win10 menus wheel-scroll on overflow anyway; if it passes, one WH_GETMESSAGE hook can carry wheel+RButton+PgUp/PgDn shaping alone).
Round 2 probe descriptions (p7-p11, in merge-probes/): **P7** WH_CALLWNDPROC (same-thread) sees WM_MENUSELECT — a SENT message MSGFILTER cannot see — and can it update a tooltip GUI from the raw callback (CWPSTRUCT is reverse-ordered: lParam@0, wParam@PtrSize, message@2·PtrSize, hwnd@3·PtrSize); **P8** AHK `Menu` rebuild from that callback at WM_INITMENUPOPUP time (JIT viability); **P9/P10** in-menu wheel via MSGF_MENU message-rewrite vs eat+PostMessage-to-owner (both also report whether wheel traverses MSGF_MENU at all; both zero → WH_GETMESSAGE); **P11** OUT-OF-CONTEXT SetWinEventHook(EVENT_OBJECT_FOCUS) — the canonical screen-reader mechanism, delivered during the modal loop's own message retrieval; fallback for P7's display half (in-codebase precedent: taskbarInterface uses SetWinEventHook). Also verify during pen testing: whether swallowing vs dispatching removed WM_POINTER messages affects legacy promotion (see C7 pen shim).

### Phase A — collision resolution, still two-threaded (app behavior identical; ~1.5-2.5k line diff)
While two interpreters exist, twins are **converged or renamed, never deleted** (each side must keep every function it calls; deletion happens at C):
- **A1 — named GUIs**: MI Gui `1→PVwin`, `2→PVgdiPic`, `3→PVgdiThumbs`, `4→PVgdiInfos`, `5→PVgdiSelect`. Sweep `Gui, N:`/`GuiControl, N:`, `+Owner1`→`+OwnerPVwin`, `1GuiClose:`→`PVwinGuiClose:`, `GuiSize()`→`PVwinGuiSize()`, `GuiDropFiles()`→`PVwinGuiDropFiles()`, `Gui, 1: Menu`. **Delete** `Gui, 1: Default` sites (e.g. MI:1167) — prefix subsequent GuiControls explicitly instead; a carried default is the landmine. Fix `PreventKeyPressBeep`'s `IfEqual, A_Gui, 1` (MI:1919) → `If (A_Gui="PVwin")`; update `preventSillyGui` name strings.
- **A2 — GUI/menu name dedupe**: MI's `mouseToolTipGuia` → `uiMouseTipGuia` (+ its 2 labels); MI's `PVmenu` → `PVbar` (MI:2266/2281/2294/2297/2516/2530). `PVmanu` keeps.
- **A3 — function renames** (class "different job" ×18, "parallel-evolution" ×6 treated the same, + role-drifted lib twins): prefix `ui` (`uiWinClickAction`, `uiKeyboardResponder`, `uiWM_KEYDOWN`, `uiWM_MOUSEMOVE`, `uiWM_LBUTTONDOWN/UP`, `uiPreProcessKbdKey`, `uiUpdateUIctrl`, `uiChangeMcursor`, `uiMouseTurnOFFtooltip`, `uiMouseCreateOSDinfoLine`, `uiShowOSDinfoLineNow` if reclassified, `uiKmenuBar`, `uiInitGuiContextMenu`, `uiShowSysMenu` for MI's `Win_ShowSysMenu`, …). **Sweep must cover string references**: `SetTimer, <name>` (MI:2483!), `Func("<name>")`, dynamic `%funcu%()` fed from `menuArray` (MI:2387/2458), and main-side `ahkPostFunction("<name>")` strings — both sides change in the same commit. Gate: zero repo-wide grep hits per old name.
- **A4 — twin convergence**: identical (8) + near-identical (5) + identical-lib (7): make bodies byte-identical (authoritative side = most recent substantive fix per `git log -L`), tag MI copy `; MERGEDEL`, runtime-test two-threaded. `msgBoxWrapper` is *absorbed*, not renamed: main's `simpleMsgBoxWrapper` (QPV:33368) already contains the native-MsgBox body; at C its iface-delegating Else branch (QPV:33433) becomes the same native MsgBox with `Gui, PVwin: +OwnDialogs`, and MI's two callers (MI:1818, 1040) retarget it.
- **A5 — main-side bare-Gui hygiene**: prefix the ~22 bare `Gui, +Option` lines (QPV:32925, 40679, 46727, 52709-53236, 57821-57824, 59389, 59969-60082, 89199-89226, 102524) with explicit GUI names; static-audit that every function with bare `Gui, Add` sets a named Default earlier in the same function.

### Phase B — facades (app behavior identical; ~700-line diff)
- Main side: `IF_call(fn, args*)` / `IF_post(fn, args*)` / `IF_set(name, val)` / `IF_get(name)` wrapping `interfaceThread.*`; MI side: `MT_post/MT_get/MT_set` wrapping `MainExe.*`. Normalize property-form `ahkgetvar.name` to call form. Rewrite all 315 sites; delete commented boundary lines. Verify variadic pass-through on Windows once.
- Land early D-item: `determineLClickState()` drops the `ahkgetvar` read → pure `GetKeyState("LButton")` (it's already the OR'd condition; `slideShowRunning=1` short-circuit stays).
- Gate: `interfaceThread\.|MainExe\.` hits only inside the 7 facade bodies.

### Phase C — the flip (one branch, one Windows checkpoint; ~2-4k diff; MEDIUM-HIGH risk, enumerated)
- **C1** MI restructured into a pure include: auto-exec (MI:1-109) → `initInterfaceModule()` (dispatcher registrations, `hasPenPressureAPI` detect, QPVpid; **drop `setPriorityThread(2)`**). MI's Global block dissolves: names already super in main deleted; the ~45-60 iface-only survivors (menu state incl. `menuArray`/`menuHotkeys`, hPic0-11 + accessibility control vars, hFlyOut/hFlyBtn1-3, taskBarUI, penPressureRaw, LbtnDwn, lastLclick*, tooltip cluster, lastCloseInvoked, navKeysCounter, prevFullIMGload…) move into main's Global section with provenance comments (existing state relocated — satisfies the no-new-super-globals rule; every name any facade writes dynamically MUST be in the Global block or `%name% :=` writes a dead local).
- **C2** `#Include` the module at the **bottom** of QPV (it contains labels; never mid-auto-exec).
- **C3** Replace QPV:454-480: `initInterfaceModule()` + `initGUI := BuildGUI()` (keep the `"|"` hwnd contract + `handleUIhwnd`); delete the `;@Ahk2Exe-AddResource` line and the GetRes branch. `BuildGUI` de-parameterized (externObj + MT_get fallback deleted → restart bug dies).
- **C4** Facade body swap — **corrected for the v1.1 variadic-method-call limitation discovered at the B checkpoint** (args* may appear in function DEFINITIONS and plain/dynamic FUNCTION calls, but expanding it into any object METHOD call — including `.Call(args*)` and `.Bind(args*)` — is a load-time "Invalid value" error): `IF_call` → `Return %funcName%(args*)` (dynamic function call, legal form); `IF_post`/`MT_post` → `SetTimer, % Func("IF_postRelay").Bind(funcName, args), -1` where `.Bind` takes two PLAIN arguments (the args array passed as one object, no star) and `IF_postRelay(funcName, argsArr)` does `%funcName%(argsArr*)` (**preserves queued/new-pseudo-thread semantics — do not inline to direct calls in this phase**); `IF_set/IF_get` → direct global write/read (echo-writes; deleted in E). Verify both legal forms in the C smoke immediately (they run at startup).
- **C5** OnMessage unification — one dispatcher per colliding number, registered once; both old blocks deleted. Return-value contract: non-empty return suppresses further processing, so sequential ui-first with `If (r!="") Return r`, else fall through to the main handler (both sides already self-guard disjointly: MI guards on the PVwin/hGDI* window set, main explicitly excludes PVhwnd). For 0x100/0x104 **branch, don't chain** (both bodies write `hotkate` and arm timers): route by `GetAncestor(hwnd, GA_ROOT) ∈ {PVhwnd, hGDI*, hGuiTip, hFlyOut}` → `uiWM_KEYDOWN` else `WM_KEYDOWN`. Re-register `PreventKeyPressBeep` for 0x101-0x103/0x105-0x108 only (today's effective state). Audit: ui-side paths reachable with non-UI windows must `Return` empty, never `0`.
- **C6** Gui-default belt: `Gui, dummyGuia: Default` at the top of each PVwin event entry (PVwinGuiSize/Close/DropFiles, `invokeMenuBarItem`, dispatchers falling into ui bodies — ~10 sites), so stray bare `Gui,` lines hit a harmless GUI, never the viewport.
- **C7** Liveness shims (ship-blocking, exit-safety): `pumpUIevents()` = `Critical, Off` → `Sleep, -1` → restore, inserted at the former poll checkpoints (QPV:74273/75731/75803/75828/75885, 85687, 48591) and inside the rewritten `determineTerminateOperation()` (pump; the abort dialog now runs directly as the interrupting pseudo-thread while the worker stays suspended — spin-wait and cross-thread dance deleted; `Return mustAbandonCurrentOperations`). **Pen shim** in `getBrushPenPressure()`: `PeekMessage(PM_REMOVE)` filtered to 0x0245-0x024A, read pressure from the message pre-dispatch via shared `readPenPressure(wp, msg)` (refactored out of `WM_PENpressure`), then `DispatchMessage` to keep DefWindowProc's legacy promotion alive (probe-verify). **P0**: `identifyMenus()` DHW fix (IsWindowVisible check) so the reader `#If` block can't go system-wide.
- **C8** Exit rewrite: `PVwinGuiClose:` → `byeByeRoutine()` verbatim (posts become direct calls; msgBoxWrapper→simpleMsgBoxWrapper); final branch = `SetTimer, TimerExit, -8000` watchdog + **synchronous `TrueCleanup()`** (fixes the COMMIT race; accepted trade: worst-case exit slower than today's instant kill). Busy/force branch sets the abort flags + 4s watchdog and lets the worker unwind through checkpoints. `exitAppu`'s post → inline watchdog; `dummyTimerExit` deleted; `TimerExit` kept as last resort. Accepted: a loop with no pump is Task-Manager-only — which is why C7's checkpoint coverage is exit-safety, not UX.
- **C9** Slideshow collapse: delete `initSlidesModes`/`infosSlideShow` (+ call sites QPV:11039/12425); `slideshowsHandler`/`dummySlideshow` become direct calls; `theSlideShowCore` stays a named one-shot timer whose three nav posts become `SetTimer, % Func("NextPicture"…), -1`; QPV:12417 becomes a one-shot BoundFunc, **not** a direct call (a direct call would run a multi-second load inside the GIF timer thread).
- **C10** Deletions: MERGEDEL twins; mirror feeders (`tlbrInitPrefs` MI:2463 + `tlbrPushPrefs` QPV:105237 — all fields are shared globals now); `MainExe := AhkExported()`; shell-stuff:1699 forward line; msgBoxWrapper echo-assigns QPV:33347/33356; `whileLoopExec` keeps its name but `setWhileLoopExec` drops the IF_set (16 iface gates now read the live global).
- **C11** Directives: MI:1-15 deleted; main's `#NoEnv/#MaxMem/DetectHiddenWindows On/#IfTimeout 35` stand; add `#Persistent` explicitly to main. **C12** DHW audit: review every WinExist/WinGet/WinActive/WinSet in MI for DHW-On sensitivity.
- Ships with **documented accepted degradations** (see Verification) — C and D1/D2 land in the same release but are separate checkpoints.

### Phase D — restoration (post-C, each independently testable; **revised per round-1 probe verdicts** — OnMessage monitors are dead during menu loops, so everything in-menu rides raw hooks)
- **D1 — native menu bar**: attach real submenus (`Menu, PVbar, Add, &File, :pvMenuBarFile`; stub-item pre-create) → native hover-switching/F10/Alt-accelerators (pure Win32, unaffected by the launch-blocking rule). JIT rebuild trigger is a same-thread **WH_CALLWNDPROC hook** matching WM_INITMENUPOPUP (message wParam = HMENU → `menuHandleMap` from `MenuGetHandle`) calling the existing `InvokeMenuBar*` builders split into build/show — viability proven by P8; if P8 fails, fallback = eager rebuild driven by `UpdateMenuBar`'s state hash (MI:2495), which then survives C. Delete `findMenuBarItemUnderMouse`, `GetMenuWinHwnd`, `SendMenuTabKey`, the F10 machinery.
- **D2 — reader (hard-mandatory in the same release as C** — p4 re-run confirmed the `#If` hotkey block at MI:2740 CANNOT function post-merge; it is dead weight after C and is deleted when D2 lands; the identifyMenus DHW P0 fix ships at C regardless so the hotkeys never leak system-wide): **mechanism PROVEN by p7 FULL PASS** — highlight tracking via the same-thread **WH_CALLWNDPROC hook** on WM_MENUSELECT (CWPSTRUCT is reverse-ordered: lParam@0, wParam@PtrSize, message@2·PtrSize) → decode item/state (names via GetMenuString on the message's HMENU) → `constantMenuReader`'s message format → `uiMouseCreateOSDinfoLine` + halo via `GetMenuItemRect` on the visible `#32768`; tooltip-GUI-from-the-raw-callback is confirmed working (guard against hook re-entry with a static busy flag — Gui commands send messages the hook then sees). **p11 (out-of-context SetWinEventHook EVENT_OBJECT_FOCUS) also fully passed** and stays the documented alternative. In-menu input shaping: RButton-announce and PgUp/PgDn provably traverse MSGF_MENU (p5 census: keydown 8, rbutton 2); **wheel does NOT** — p12 (WH_GETMESSAGE) also failed, and **p13 PASSED: the wheel mechanism is a menu-scoped WH_MOUSE_LL hook** — while a visible own-process `#32768` exists, eat the hardware wheel event and post WM_KEYDOWN Up/Down to the owner (p13 also proved menus accept queue-posted key-downs — usable for PgUp/PgDn shaping too). Install LL only while a menu is open (0x211/0x212 via CALLWNDPROC), keep its callback minimal (compare/post/return — system-wide hook latency budget). No feature is dropped. Plain SendInput from hook callbacks is confirmed ineffective — never design on it. Hooks installed at menu open (CALLWNDPROC sees WM_ENTERMENULOOP 0x211 too), removed at WM_EXITMENULOOP 0x212.
- **D3 — sqlite cancel**: `sqlite3_progress_handler` via RegisterCallback on the AHK-owned connections (`VACUUM` QPV:73626, big GetTable queries) — same-thread callback checks Esc/`mustAbandonCurrentOperations`, returns nonzero to abort. Callback kept to GetKeyState + global reads (no GUI calls); direct sqlite3 DllCalls per the Class_SQLiteDB traps memory. **Dupes-engine contingency (verified):** `dupesQueryBegin` (QPV:87479) takes SQL text and no connection handle — the DLL holds that connection internally, so AHK cannot register a handler on it. Either add a minimal DLL-side progress handler (the one potential qpvmain.dll change in this plan; ship .cpp+.ahk together per the ABI rule) or document the sort phase as uncancelable-mid-step (the stepping loops' existing `dupesEngineCancel` checkpoints QPV:36711+ still cancel between steps).
- **D4** — pen cadence polish; taskbar-during-busy re-check; `hideMenuFlyOut` behavior inside menus.

### Phase E — de-H toolchain + cleanup (LOW risk)
Stock AutoHotkey v1.1 (U64/U32) for uncompiled runs; official Ahk2Exe (plain `#Include` compiles natively); bump `#Requires` (QPV:53); README:237 + About string (QPV:89229); delete `lib/module-fim-thumbs.ahk`; `detectToolbar` callers → `adjustCanvas2Toolbar` + delete twin + its static bridge; delete `GetRes` if the QPV:457 branch was its only caller (re-verify); delete IF_set/IF_get echoes, optionally inline trivial IF_call/IF_post sites; archive `ahk_h-v1-1-33-10-h005.zip`; changelog.

## Verification

**Linux static gates (per phase; exclude .claude/worktrees/):** collision detector over parsed function definitions across QPV/MI/shell-stuff/Gdip_All/msgbox2 — A-gate: every remaining collision MERGEDEL-tagged and byte-identical; C-gate: empty. Boundary counters — B-gate: bridge calls only in facade bodies; C-gate: zero repo-wide, and zero `ahkthread(`/`AhkExported(`/`Ahk2Exe-AddResource`. Gui gates — A: zero `Gui, [0-9]:` in MI; C: bare-Gui audit clean. Rename sweeps: zero old-name hits per symbol (incl. SetTimer/Func("")/menuArray/post-string forms). **Super-global invariant (C):** every name any facade or dynamic assign touches appears in the merged Global block (the known v1 dead-local trap). Directive/label uniqueness scans; `#NoEnv` env-name audit of MI (verified clean pre-merge; re-run merged). DHW audit checklist. **Optional→required if it works: Wine load-pass** `AutoHotkeyU64.exe /iLib nul /ErrorStdOut quick-picto-viewer.ahk` — v1's loader rejects missing/duplicate functions and duplicate labels before Windows testing.

**Windows manual checklists:** A: start, no key-beep over main window, both tooltip styles, borderless drag, halo, menus+bar+flyout, slideshow, F10 bar hide (`PVbar`/`PVmanu` swap), exit. B: A + drag-paint during slow filter, GIF, abort during RAW/PDF load, dupes cancel, taskbar buttons, `handleFatalWinInitErrors` restart, toolbar reposition, multi-monitor. C (full matrix): all above + pen pressure varies within a stroke in both brush loops and resets after lift; touch pan/swipe; NVDA over panels and popups; slideshow + FX randomizer; GIF nav-kill; thumbs scroll-abandon; color-picker Escape; **all byeByeRoutine veto stages ending in a clean (non-killed) exit with the seen-images DB committed**; force-exit during a long op; GUI-restart passes colors/fonts; dark mode toggle; Win7/8.1 box if available (QPV:5854 branch). **Accepted degradations at C (tell testers):** bar hover-switching dead until D1; OSD menu reader dead until D2 (NVDA's own reading works); chrome/taskbar unresponsive during un-pumped stretches; taskbar thumb-button clicks queue while a menu is open (their OnMessage monitors now live in the interpreter that is tracking the menu — today the interface thread serves them during main's menus); sqlite sort/VACUUM uncancelable until D3; truly-hung loops are Task-Manager-only. D: hover-switching across all 13 bar items in all 6 contexts (menusList MI:2241-2246); JIT dropdowns reflect live state; reader announcements incl. disabled/checked/submenu; wheel/PgUp/RButton/Space/Tab in menus; Left/Right with dropdown open; Esc aborts VACUUM + dupes sort. E: uncompiled under stock v1.1, compiled with official Ahk2Exe, oldest supported Windows.

**2026-08-31 — PHASE C EXECUTED (branch `interface-thread-merge-phase-c`).** The flip is complete: one interpreter, zero ahk-h APIs. ~918 deletions / 414 insertions across quick-picto-viewer.ahk, module-interface.ahk, shell-stuff.ahk.
- C1: MI head → top-of-file `Global` declaration (66 module-owned names; the 84 shared duplicates deleted — main authoritative) + `initInterfaceModule()` (state seeds incl. `otherAscriptHwnd := A_ScriptHwnd`; registrations; beep suppression on 0x101-0x103/0x105-0x108 matching pre-merge effective state; setPriorityThread and the tray line dropped). Accidental same-names renamed: `uiMouseTipWinCreated`, `uiLastTippyWin` (the two tooltip stacks were about to share state). `FontBolded` → `OSDfontBolded` (externObj field-16 name mismatch found at de-param).
- C2/C3: `#Include` at the BOTTOM of QPV; startup block = `initInterfaceModule()` + `initGUI := BuildGUI()`; AddResource directive + GetRes branch + externObj handshake deleted; BuildGUI() de-parameterized (both redundant RegReads dropped; the 9-of-16 restart bug dead); restartEntireGui() de-parameterized.
- C4: star-free facade bodies — IF_call = arity-dispatched dynamic `%funcName%()`; IF_post/MT_post = `SetTimer` one-shot with `Func("IF_postRelay").Bind(funcName, args)` (array as ONE plain arg) + shared relay; IF_set/IF_get = Global-mode dynamic access; MT_get/MT_set delegate.
- C5 **design upgrade during implementation**: dispatchers BRANCH by window root (`isUIrootWin` = PVhwnd + 4 GDI + hGuiTip + hFlyOut), never chain — each side's guards assume its pre-merge message universe (verified: uiWM_MOUSEMOVE would have processed panel messages). The 5 module-only registrations (RButton/MButton/mouseleave/pos-changed/activate) got the same universe guards.
- C6: the `Gui, dummyGuia: Default` belt was **deliberately skipped** — A5's audit found zero functions with bare Gui commands lacking an in-function Default, and queued posts don't inherit the PVwin default; insurance declined, recorded here.
- C7: `pumpUIevents()` (explicit Critical restore — `Critical, 0` means ON, never pass it) full-pumps ONLY in the rewritten `determineTerminateOperation()` (its old spin slept=pumped, so the re-entrancy surface is unchanged; the abort dialog now runs inline). `drainUIinput()` (selective PVwin-family drain, keys+mouse, 40-message cap, static re-entrancy guard, direct handler calls — no timers/posts fire) at the 5 load checkpoints + thumbs loop + color-picker loop. `pumpPenMessages()` (pre-dispatch read + DispatchMessage for promotion continuity, Critical save/restore around the handler's own `Critical, off`) inside getBrushPenPressure. `identifyMenus`/menu probes → `uiVisibleMenuWin()` (the hidden-#32768 fix, applied at 4 sites incl. a WinGet-List count filter).
- C8: exit — final branch runs `TrueCleanup()` SYNCHRONOUSLY with `TimerExit` as an 8s watchdog (the 10ms COMMIT race is dead); busy/force-exit "yes" = flags + queued cleanup + watchdog + a **detached `taskkill /PID /T /F` after ~8s grace** (the only guaranteed force-exit on one thread — a truly stuck DllCall pumps nothing, so in-process timers can't fire; the taskkill no-ops if the clean path won). `dummyTimerExit` deleted.
- C9: `initSlidesModes`/`infosSlideShow` (pure mirrors) + call sites deleted; everything else rides the queued facades unchanged.
- C10: all 30 MERGEDEL twins deleted (+ main's dead PreventKeyPressBeep copy); **msgBoxWrapper absorb resolved a REAL duplicate-definition** (main has its own 14-param msgbox2-based `msgBoxWrapper`; MI's native 7-param copy deleted, its 2 callers → `simpleMsgBoxWrapper` — exact signature match; simpleMsgBoxWrapper's Else-delegation branch neutralized); tlbrInitPrefs/tlbrPushPrefs machinery deleted (5 sites); shell-stuff's setMenusTheme forward line deleted (post-C4 it would have been an infinite timer loop).
- C11: MI directives gone; `#Persistent` added to main. C12: DHW audit run — `WinActive` is DHW-immune, ahk_id ops only get more reliable, the four #32768 probes fixed via uiVisibleMenuWin.
- **Arity gate (new, advisor-mandated)**: post-merge, dynamic calls enforce arity that the cross-thread API never did. Two real violations found and fixed: `TrueCleanup` takes zero params (both new call sites corrected) and the wheel-path `KeyboardResponder` post passed 3 args to a 4-required function (now passes navKeysCounter). All other 56 facade targets verified arity-clean.
- **All gates green**: zero duplicate functions/labels across the merged namespace, zero unresolved facade/OnMessage/SetTimer targets, zero undeclared IF_set names, zero bridge APIs, brace-neutral vs HEAD, CRLF+BOM intact.
- **Known behavior deltas to observe on Windows** (correct-direction or accepted): MI gates that read never-assigned mirrors (e.g. `imgEditPanelOpened` in uiIsNowAlphaPainting) were dead pre-merge and are LIVE now; debounce cookies like lastOtherWinClose are single-truth (timing shifts); force-exit on a truly stuck operation takes ~8-9s instead of 10ms; plus the planned accepted degradations (hover-switching/reader until D, taskbar buttons during menus, un-pumped stretches).

**2026-08-31 — PHASE C HALTED: runtime regressions unresolved; Marius stopped the work.**
- On the phase-c build: slideshows advance once then stop; GIF playback flickers and cannot be interrupted. Startup, image loads, thumbnails, folder-scan abort and pen-pressure painting all work. The stale-hotkate drain fix (64be719) — real bug, correctly described — did NOT cure the symptoms.
- **State of the branches:** `master` untouched and good. `interface-thread-merge-phase-a` (through commit bcfe902) passed Marius' full smoke test — collision-free renames + facades, still two-threaded; standalone value even if the merge never resumes. `interface-thread-merge-phase-c` is NOT usable; treat as experimental quarantine.
- **If ever resumed, do NOT symptom-guess again. Instrument first**: add journal/OutputDebug logging to autoChangeDesiredFrame (per-tick: act, animGIFplaying, allowNextSlide, thisFrameDelay), theSlideShowCore (gate values), dummySlideshow (whether it re-arms), drainUIinput (what it drained), and every writer of animGIFplaying/allowNextSlide/canCancelImageLoad — one user run then yields the whole story. Also bisect by no-op'ing drainUIinput entirely.
- **Remaining suspects, most plausible first:** (1) the re-arm/kill paths moved from an always-responsive second interpreter onto queued relay timers and deferred monitors — under the GIF tick chain's near-continuous Critical, their ORDER and timing differ from the old posted-message semantics even where each piece is individually correct; (2) `autoChangeDesiredFrame`'s per-paint re-entry via QPV_ShowImgonGui's unconditional "start" call interacts differently now that all flags are single-truth (pre-merge the iface copies lagged; post-merge every intermediate write is instantly visible to the gate reads); (3) monitor starvation blocking the click/key kill paths during 1-10ms adaptive frame delays; (4) the per-frame drainUIinput cost/side-effects at the load checkpoints (only partially exonerated by 64be719).
- The lesson recorded for any future large refactor here: with the app un-runnable on this box, every regression round-trip spends Marius' time — ship instrumentation WITH the change, not after the third symptom report.

**2026-09-01 — RESUMED at Marius' request ("resume to phase d and e"); D and E EXECUTED.**
- Marius adopted the phase-c base (his own brush-tools consolidation committed as eca39d0). The C-era slideshow/GIF regression status is UNKNOWN — not re-litigated; instead, per the halt protocol, commit 3e9885d ships unconditional `QPVMERGE:`-prefixed OutputDebug tracing at every point the halt entry named (slideshow core/handler/rearm, GIF branch transitions, drain activity, keyboard dispatch, both kill functions log their CALLER via Exception(-2)) — one DebugView capture now tells the whole story.
- **D1+D2 (138b6b0)**: menu bar carries real attached submenus (native hover-switching/F10/Alt); JIT dropdown rebuilds on WM_INITMENUPOPUP via a permanent WH_CALLWNDPROC hook; reader announcements from WM_MENUSELECT (GetMenuString + flags, no MSAA/COM in hooks); menu-scoped WH_MOUSE_LL for wheel + RButton re-announce. Builders gained `justBuild` (uniform tails; VectorView special-cased). **Load-bearing enabler: every whole-menu Delete of an attached menu became DeleteAll** (11 builder tops + deleteMenus' list split) — a whole Delete orphans the HMENU behind the bar attachment and the JIT map. Deleted as unable-to-function post-merge: the reader #If hotkey block, the 60ms MSAA poll, F10 machinery, invokeMenuBarItem, constantMenuReader + the whole Acc_* cluster, and friends.
- **D3 (2bc4cb1)**: sqlite3_progress_handler armed on every Class_SQLiteDB connection (registered in OpenDB); callback = flag reads + async Escape probe; gated on runningLongOperation or allowSQLiteAbort (brackets the 3 VACUUMs). Dupes engine's DLL-internal connection stays out of reach (documented).
- **E (9fd89c6)**: stock-AHK v1.1 + official Ahk2Exe declared (README + About); GetRes deleted; **IF_set/IF_get/MT_set/MT_get retired** — all 180 sites are plain global assignments/reads (zero mangles; membership verified; the two never-super names are comment-only). detectToolbar/adjustCanvas2Toolbar deliberately NOT unified (contract mismatch; sync hazard already gone) — comments updated. IF_call/IF_post/MT_post + relay stay (dispatch semantics).
- Gates at every step: zero duplicate defs, zero unresolved facade/OnMessage/SetTimer/RegisterCallback targets, zero bridge refs, CRLF+BOM intact.
- **Windows test list for this push** (lead items): the menu matrix across ALL SIX contexts — view/editor/alpha-mask/vector/thumbs/welcome — switching between them (the JIT map rebuilds on every BuildMenuBar; stale-map symptom = dropdowns from the previous mode); hover-switching across the bar with a dropdown open; Alt+letter opens each menu EXACTLY ONCE (double-fire would implicate main's menuHotkeys consumer at QPV:990); dropdown content freshness (undo counts, checkmarks) right after state changes; with allowMenuReader on: announcements follow highlight (mouse + arrows), RButton re-announces, wheel moves the highlight; wheel in an overflowing menu; Escape during a VACUUM aborts the statement; the C-matrix regressions (slideshow multi-advance, GIF interrupt) re-checked — **any misbehavior report should come as a DebugView tail (filter QPVMERGE:), not a symptom description.**

**2026-09-01 — post-D/E fix trail (all pushed):** 535ab8a slideshow speed corrupted after first run (stop passed 0 into what is now the shared preference — split into module-owned slideShowCadence); 9d47403 context-menu fixes (reader announces on RButton only, like the old hotkey; JIT gated to bar sessions via WM_ENTERMENULOOP wParam); f6a3b99 bar menus dying after the context menu (DestroyMenu recursion through PVmenu's attached submenus — deleteMenus empties parents first + uiRefreshBarAttachments self-healing net); 9342ed1 phantom taskbar window (uiRepositionWindowCenter called with literal gui number 1 — dynamic %whichGUI% created an anonymous visible Gui titled with the app name; renames cannot see numbers passed as arguments); 642f8ca menu flyout never showed (its 25ms AHK display timer cannot tick in the modal loop — replaced with a native Win32 TIMERPROC ticker, which the loop dispatches). Marius-validated so far: startup, loads, thumbnails, scan-abort, pen painting, GIF playback+interrupt, slideshows incl. speed persistence, menu bar + keyboard shortcuts, context menu. Marius' own commits on the branch: eca39d0 (brush consolidation), 047b148 (Class_taskbarInterface.ahk extracted from the module).

## Explicit punts (not in this plan)
Async current-image decode via the thumbs-pool `wantBitmap` mode (the #1 freeze source — future project); GetFilesList/thumbs-harvest/fade re-architecture beyond pump shims; inlining posts beyond the mechanical BoundFunc mapping; pen support below Win8; multi-core worker changes (already de-H-clean); tooltip-cluster/native-msgbox consolidation (E option only).

## Execution log

**2026-08-31 — Phases P and A executed (working tree, uncommitted).**
- **P**: `merge-probes/` created — p1-p6 probe scripts + README. Run on Windows with stock AHK v1.1 before Phase C; record the six verdicts.
- **A1**: MI Gui 1-5 → PVwin/PVgdiPic/PVgdiThumbs/PVgdiInfos/PVgdiSelect (119 refs); 8 `Gui, 1: Default` lines deleted (no bare GuiControl relied on them); labels → `PVwinGuiClose:`; `GuiSize`/`GuiDropFiles` → `PVwinGuiSize`/`PVwinGuiDropFiles`; `+Owner1` → `+OwnerPVwin`; PreventKeyPressBeep A_Gui compare → "PVwin".
- **A2**: MI `mouseToolTipGuia` → `uiMouseTipGuia` (18 refs + 2 labels); MI menu `PVmenu` → `PVbar` + the 7 main-side `setMenuBarState(...,"PVbar")` arg strings.
- **A3+A4-renames**: 26 MI functions renamed with `ui` prefix (incl. reclassified `uiIdentifyThisWin`, `uiIsNowAlphaPainting`; `Win_ShowSysMenu`→`uiShowSysMenu`); 7 `A_ThisFunc` posts literalized first; sweeps guarded with `/MainExe\./!` so post-strings targeting main kept their names; main-side `"changeMcursor"` posts → `"uiChangeMcursor"` (QPV:100409/100412).
- **A4**: 33 twin pairs converged byte-identical (MI→lib/QPV text for 15; `calcScreenLimits` mouse-pos fallback backported MI→msgbox2; `PreventKeyPressBeep` main-copy converged to MI's). Tags: 30 `; MERGEDEL` + 1 `; MERGEABSORB` (msgBoxWrapper) + 1 `; MERGEKEEP` in MI, 1 inverse `; MERGEDEL` in QPV. setMenusTheme tag notes the shell-stuff:1699 forward line to delete at C.
- **A5**: 22 bare `Gui, +Option` lines prefixed (21 `SettingsGUIA:`, 1 `%A_Gui%:` in OnLButtonDblClk); `uiADDalphaMaskTabs` got an explicit `Gui, SettingsGUIA: Default`; bare-Gui audit = 0 exceptions.
- **Gates all green**: collision detector 32/32 tagged; new ui* names clash nowhere; each defined once; no dup labels; all SetTimer targets resolve; cross-thread name resolution clean both directions; CRLF/BOM intact (MI is CRLF+BOM, QPV is LF+BOM — tooling must stay ending-aware).
- File stats: module-interface.ahk 592 lines changed, quick-picto-viewer.ahk 68, msgbox2.ahk 7.
- **Next**: Windows manual test per Phase-A checklist, run the probes, then Phase B (facades).

**2026-08-31 (later) — Phase A validated on Windows; probe round 1 run; round 2 shipped.**
- User confirmed the full Phase-A smoke checklist passes: startup, menus, tooltips, thumbnails, toolbars, panels, keyboard shortcuts, flyout, slideshows. **Phase A checkpoint is green; branch `interface-thread-merge-phase-a` on GitHub (a850285 + plan 4694333).**
- Probe round-1 verdicts recorded (see Phase P section): the empirical launch-blocking rule is established; P5 proved raw hooks are the in-menu vehicle. D1/D2 revised onto WH_CALLWNDPROC + WH_MSGFILTER/SetWinEventHook accordingly; the reader `#If` block is now known non-viable post-merge (D2 hard-mandatory with C).
- Round-2 probes p7-p11 added to merge-probes/ (CALLWNDPROC menuselect+tooltip, CALLWNDPROC initmenupopup rebuild, wheel rewrite, wheel eat+post, WinEventHook); p4/p5 now auto-classify their verdicts.
- Open questions for the next probe run: (a) p4 — did any entries carry ticks smaller than the close tick? (b) p5 — did the log contain `wheel delta=` lines (wheel traverses MSGF_MENU) or only keydown/RButton entries?
- **Next**: run p7-p11 (re-run p4/p5 if convenient), then Phase B.

**2026-08-31 (round 2) — D1/D2 mechanisms settled by probes.**
- Verdicts: p4 FAIL (hotkeys fully blocked in-menu), p5 census wheel 0/keydown 8/rbutton 2, **p7 FULL PASS**, **p8 FULL PASS**, p9/p10 FAIL (no wheel in MSGF_MENU), **p11 FULL PASS**. D1 = native submenus + CALLWNDPROC JIT rebuild (proven); D2 = CALLWNDPROC WM_MENUSELECT tracking + tooltip (proven; p11 alternative also proven); wheel = p12 (WH_GETMESSAGE) added, the sole open probe.
- **Next**: run p12; Phase B can start any time (independent of probe outcomes).

**2026-08-31 (round 3/4) — p12 FAIL; final wheel probe p13 shipped.**
- p12 (WH_GETMESSAGE rewrite) failed. p13-wheel-forensics.ahk taps all remaining layers in one run: queue (PM_REMOVE + peeks), SENT messages (CALLWNDPROC), and a menu-scoped WH_MOUSE_LL hook that eats the wheel and posts WM_KEYDOWN — the counters localize where wheel input goes, the LL tap tests the last viable mechanism. Its menu-open gate uses the visibility-aware `WinExist("ahk_class #32768 ahk_pid ...")` (the hidden-#32768 trap from the P0 finding applies to probes too). Outcome either selects "menu-scoped LL hook" as the D2 wheel mechanism or retires the in-menu wheel feature as an accepted degradation (native menus wheel-scroll on overflow regardless). All other D1/D2 mechanisms remain settled; nothing else depends on this.
- **Next**: run p13; Phase B on user's word.

**2026-08-31 — Phase B executed (facades).**
- Main gains `IF_call/IF_post/IF_set/IF_get` (defined right after the ahkThread creation, QPV:~460-482); MI gains `MT_post/MT_get/MT_set` (after the auto-exec Return). All bridge sites rewritten mechanically with exact count conservation: IF_call 18, IF_post 49 (incl. shell-stuff:1699 live + comments), IF_set 168, IF_get 15→14 live, MT_post 40, MT_set 11, MT_get 15 — post-rewrite counts match baseline plus one definition each. Raw `interfaceThread.ahk*`/`MainExe.ahk*` now exists ONLY inside the 7 facade bodies (gate verified).
- `ahkgetvar` access switched from property form (`.ahkgetvar.Name`) to call form (`.ahkgetvar("Name")`) inside the facades — the one behavioral delta of the phase; verified against the AHK_H zip's AhkThread.ahk API earlier, and exercised immediately at startup (BuildGUI) and on first image load (canCancelImageLoad polls). **First Windows-checkpoint item.**
- Early D-item landed: `determineLClickState()` now reads `GetKeyState("LButton")` directly (the LbtnDwn mirror stays for the interface-side handlers).
- The `; IF_post("dummy")` example trio deleted; `lib/module-fim-thumbs.ahk` deleted (dead since the thumbs C++ pool migration; E-item pulled forward, zero risk per audit).
- Collision detector re-run: 32/32 tagged, facades introduce no new collisions; MI still pure CRLF+BOM; setMenusTheme twin pair converted consistently on both sides (known one-line diff unchanged).
- **Windows checkpoint for B**: full smoke = Phase-A list + drag-paint with LButton held (validates the determineLClickState change with mouse AND pen/touch), GIF playback + nav-kill, abort during a slow RAW/PDF load (Escape and M-click — exercises IF_get call form), slideshow start/stop + transitions, dupes-scan cancel dialog, taskbar buttons, forced GUI restart (handleFatalWinInitErrors), toolbar reposition, multi-monitor OSD, msgBox-from-interface (unsaved-changes exit prompt).
- **Next**: Windows B-checkpoint, p13 verdict, then Phase C on a fresh sub-branch.

**2026-08-31 — p13 PASS; probe program COMPLETE.**
- The menu-scoped WH_MOUSE_LL eat+post mechanism moves the highlight per wheel notch. Bonus: queue-posted WM_KEYDOWN provably drives menus. Every phase-D need now has a proven vehicle (final table in merge-probes/README.md); no feature is dropped, no accepted degradation needed for wheel.
- **Remaining before C**: the Windows B-checkpoint smoke (facades + ahkgetvar call-form + determineLClickState change).

**2026-08-31 — B-checkpoint round 1: load failure, fixed.**
- Both interpreters rejected the facades at load with "Invalid value" pointing at the standalone `obj.ahkPostFunction(funcName, args*)` statements: **AHK v1.1 does not accept args* expanded into an object METHOD call** (definitions and plain function calls are fine — the def lines parsed as context in the very error listing). The facade bodies now dispatch on `args.Length()` and forward with the exact arity of each call site (IF_call ≤7, IF_post ≤9 = the H api cap, MT_post ≤5 — censused from the sites). Behavior-preserving by construction; gate confirms zero variadic method calls remain repo-wide.
- Same limitation invalidated the PLANNED C4 bodies (`.Call(args*)`/`.Bind(args*)`) — C4 redesigned onto the legal forms: dynamic `%funcName%(args*)` for IF_call, and a relay BoundFunc (`Func("IF_postRelay").Bind(funcName, args)` — array as one plain arg) for the posts.
- **Next**: re-run the Windows B-checkpoint smoke on the fixed build.

**2026-08-31 — B-checkpoint round 2: second load failure, fixed per the user's diagnosis.**
- The arity-dispatch fix loaded in the MAIN interpreter but the thread's parse of module-interface.ahk still failed: this build ALSO rejects `args[N]` indexing directly inside method-call arguments (`MainExe.ahkPostFunction(funcName, args[1])` → "Invalid value"; plain `obj.method(funcName)` on the line above parsed as unflagged context). Notably the exe accepted what the thread's AutoHotkey.dll rejected — the two parsers differ (the bundled dll may be older than the exe). Fix (user-prescribed): hoist `args[N]` into plain locals (`a1 := args[1], …`) before the call; applied uniformly to IF_call/IF_post/MT_post. Gate: zero `args[` inside method-call argument lists repo-wide.
- Rule collected for phase C and all future facade work on this runtime: **inside object METHOD calls use only plain variables/literals — no `args*` expansion, no `x[n]` indexing; hoist first.** (Assignments and Return-expressions parse the full grammar; statement-level method calls are the restricted context, and the dll parser is the strictest.)
- **Next**: re-run the Windows B-checkpoint smoke.

**2026-09-01 — resumed D/E complete; post-E regression burn-down (user-validated on Windows unless noted).**
- Slideshow speed wrong on second run: slideshowsHandler's stop clobbered the delay preference (thisSlideSpeed=0) → module-owned `slideShowCadence` carries the cadence. GIF playback + interruption re-validated good.
- Context menu: constant reader announcements + broken reopened menus → MENUSELECT announcements are track-only (RButton re-announce via the LL hook) and JIT rebuilds gate on `barMenuSession` (ENTERMENULOOP wParam, order-proofed against 0x117-first bar delivery).
- Bar menus died after the context menu: the popup parents' whole-delete recursively destroyed the attached submenus → deleteMenus empties parents first (DeleteAll), plus the `uiRefreshBarAttachments` self-healing net after every menu loop.
- Phantom taskbar window: `uiRepositionWindowCenter(1,…)` literal gui number created an anonymous visible Gui → callers pass window names.
- Flyout, six rounds: native TIMERPROC tickers (NULL-hwnd + PVwin-bound 0xF17E — AHK timers never tick in modal loops), placement anchored by HMENU identity via MN_GETHMENU (defeats fade-out ghosts), once-per-root anchoring (bar dropdowns + a popup session's FIRST menu), 350ms `flyoutGraceZeit` so S/T/M clicks land after menu close, `allowMenuReader` seeded "yes" and no longer clobbered by setWinCloseZeit's close posts.
- Alt+letter (regression of the first attempt): un-intercepting and trusting DefWindowProc was dead — the SYSCHAR chain never completes in this focus arrangement — so `uiWM_KEYDOWN` now posts what Windows itself generates: WM_SYSCOMMAND `SC_KEYMENU` + the character → NATIVE bar tracking, Left/Right switching works (6ccbdf7). Alt+Space: same mechanism with the space char → native SYSTEM menu; supersedes the thread-era TrackPopupMenu emulation, which silently stopped producing a menu post-merge (c3ab7d4). Both validated.
- Thumbs-mode `trGdip_DrawImage` Invalid_Parameter (intermittent): determineTerminateOperation FULL-pumped (Critical off + Sleep -1), letting a queued GuiGDIupdaterResize recreate the GDI+ canvas inside QPV_ShowThumbnails' Critical loop → the checkpoint now runs `drainUIinput()` only: abort gestures inline under Critical, all other queued work stays queued until the operation unwinds (the faithful pre-merge semantics); the drain also answers the title-bar ✗ (queued WM_NCLBUTTONDOWN HTCLOSE → preByeRoutine); `pumpUIevents()` deleted so the full pump cannot return (dc94f9c). Rule: **worker-loop checkpoints may drain, never pump.** Awaiting longer-run validation.
- Still to verify: stock AutoHotkey v1.1 launch + official Ahk2Exe compile (the de-H proof, then bump #Requires), NVDA pass, touch, exit ladder + seen-DB commit, Escape-during-VACUUM, multi-monitor.

**2026-09-01 (later) — DE-H PROOF, PART 1: PASSED.** Marius runs the merged app on stock **AutoHotkey_L v1.1** on Windows: "all good, it works." The elimination goal is met at the interpreter level; the `#Requires AutoHotkey v1.1.33.01+` declaration already matches, and a reference sweep finds zero live _H dependencies (historical comments only). Remaining: the official **Ahk2Exe compile** (part 2), then his extended testing days and the matrix (NVDA, touch, exit ladder + DB commit, Escape-during-VACUUM, multi-monitor).

**2026-09-02 — animation stop was a toggle (merge-revealed regression, fixed 81ba65a + b45a214).**
- Report: a stopped GIF auto-restarted on the next action. Root cause: `autoChangeDesiredFrame`'s kill branch latched `prevAnimGIFwas` AFTER clearing `prevImgPath` (empty latch), and since the merge every ui-side stop lands there: `stopGiFsPlayback` zeroes the now-SHARED flag before posting "stop", so the posted stop's `If (animGIFplaying=1)` gate skips the correct stop branch. Master had the same reversed order, masked by the two-flag race (the posted stop usually ran while main's copy still read 1).
- Fix: latch before clearing, in both branches, guarded by `StrLen(prevImgPath)` (the kill branch can run twice — ShowTheImage re-arms the timer after a "start" that took the kill path); the stop gate widened to `flag=1 || StrLen(prevImgPath)` so the posted stop tears down again, timer-off included; `restartGIFplayback` (X key, Ctrl+click) lifts the latch itself; the dead `animGIFplaying=-1` auto-resume tail in KeyboardResponder removed; `gifStart` log names its caller.
- **Class to audit for:** any handler that runs *after* a ui-side function already flipped the shared flag and gates on `flag=1` was a real gate pre-merge and is a no-op now. Semantics kept: navigate-away-and-back auto-plays per preference (latch is about the current image).

**2026-09-02 — ui-twin consolidation pass (9d2f8be, per Marius).** Inventory in `interface-merge-renamed-functions.md` §6. `uiConstructKbdKey` + dead `uiAddJournalEntry` deleted; `uiIdentifyThisWin` → `identifyThisWin` (write-only `otherAscriptHwnd` removed); `uiIsAlphaMaskWindow`/`uiIsNowAlphaPainting` → main's predicates (panels 74/89 count on the UI side; `isImgEditingNow()` replaces the mirrored flag pair — the alpha-masking menu-bar chooser now switches on that condition); the tooltip pair → ONE window `mouseToolTipGuia` (module's OSD-font/bold + margin adopted for all tooltips, main's no-Critical choice and click handler kept), `mouseTurnOFFtooltip` took the module's richer body and replaced `uiMouseTurnOFFtooltip` at 14 sites — `hGuiTip` has a single owner now (both windows used to write it). Behavioural consequences to watch: panel/toolbar tooltips render in the OSD font; any hide from either side hides *the* tooltip. Still open: the dead Alt+Space emulation trio.

**2026-09-02 (later) — follow-up per Marius:** tooltips always Arial Bold with main's 1.25× margin (the OSD-font adoption reverted); the dead Alt+Space emulation cleared — `uiShowSysMenu` + `coreShowSysMenu` + the unreachable `!Space` branch of `uiKeyboardResponder` deleted, `Win_ShowSysMenu` in shell-stuff.ahk kept by request as the library helper. Alt+Space itself stays on the SC_KEYMENU post in `uiWM_KEYDOWN`.

**2026-09-02 (later) — wrapper clean-up, per Marius (group A + `IF_call` only):** `identifyMenus` → `uiVisibleMenuWin()` at both sites; `sendWinClickAct` → `MT_post("WinClickAction", …)` at both sites; `uiRepositionTempBtnGui`/`uiSaveMainWinPos` timer relays → the timers target `RepositionTempBtnGui`/`saveMainWinPos` directly (one queued hop instead of two); `IF_call` retired — its 11 literal-name sites are plain calls, every arity verified against the target first. `IF_post`/`MT_post` untouched by request. Module at 107 functions.

## Critical files
`quick-picto-viewer.ahk`, `lib/module-interface.ahk`, `lib/shell-stuff.ahk` (16 collisions + GetRes + setMenusTheme), `lib/Gdip_All.ahk` (MDMF_*), `lib/msgbox2.ahk` (calcScreenLimits). No qpvmain.dll changes expected; the sole contingency is D3's dupes-engine progress handler (DLL-internal connection).
